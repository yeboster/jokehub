# Project Progress: Joke Hub

## DONE
*   **Core Setup**: Next.js, ShadCN, Tailwind, Firebase, Genkit initialized.
*   **Authentication**: User signup, login, logout, auth context.
*   **Joke Management**:
    *   Manual Joke Addition (with form, AI assist option).
    *   CSV Joke Import.
    *   Joke Listing with Filters (scope, category, rating, used status) & Pagination.
    *   Individual Joke Viewing Page (layout per mockup, public access).
    *   Joke Editing (text, category, owner rating, 'used' status).
*   **AI Integration**:
    *   AI-powered joke generation for "Add Joke" page.
*   **Rating System**:
    *   Users can submit/update 1-5 star ratings and comments for jokes.
    *   Joke detail page displays "Your Rating" section.
    *   Joke detail page displays "Community Feedback" section (other users' ratings, average rating).
    *   Display of average rating (read-only, or "No ratings yet") on joke list items.
    *   Firestore migration for initial ratings data.
*   **UI/UX**:
    *   Theming with specified colors.
    *   Responsive Navbar.
    *   Toast notifications.
    *   Home page displays latest 3 public jokes with `JokeListItem`.
    *   Joke Detail Page layout updated as per mockup.
    *   `JokeListItem` layout updated to show average rating or "No ratings yet".
*   **Context Management**: `JokeContext` and `AuthContext` are functional.
*   **Error Fixes**: Various bug fixes (e.g., icon imports, public joke viewing).

## WORKING
*   Implementing a context persistence mechanism for the AI assistant (creating summary files).

## DONE (2026-08-15, improvement round 2)
*   Security: hardcoded Firebase UID removed from `/api/jokes/add` → `JOKEHUB_JARVIS_USER_ID` env var (must be set in Vercel env + local `.env.production.local`).
*   Perf: delta-based rating aggregation (`ratingSum`/`ratingCount` on joke doc, 2 reads per submit, was N+1); average rating single-sourced from joke doc; exemplar fetch cached 60s; home page fetches `limit: 3`.
*   Hygiene: dead `my-jokes` route + 12 orphaned components deleted; Vitest setup with 21 unit tests (`npm test`).
*   Infra: `firestore.rules` + `firestore.indexes.json` + `firebase.json` at root; `npm run firestore:deploy` (rules NOT yet deployed); composite-index queries fall back to client-side sort when the index is missing.

## DONE (2026-08-17, improvement round 3)
Driven by the Opus 5 audit `docs/audit-2026-08-17.md` via `docs/plans/2026-08-17-improvement-round-3.md` (9 tasks, one commit each).

*   **Security (§1.3, §1.4)**: both AI routes now require auth (Firebase ID token or `JOKEHUB_API_TOKEN`) and are rate-limited; explanations generate on explicit click instead of on page view; constant-time token compare and strict `Bearer` parsing; auth-infra failures return 500, bad credentials 401.
*   **Firestore (§1.1, §1.2, §1.5–§1.8, §1.11)**: ratings-delete rule gained a joke-owner branch; composite indexes declared; rating-aggregate writes bounded in the rules; deletes/imports chunked at ≤500 writes; timestamp mapping made total (`toDate`/`toMillis`); source length validated client-side.
*   **API hardening (§1.9, §1.10)**: `/api/jokes/top` clamps its query params (`limit` 1–50, `minRating` 0–5, NaN-safe) so a hand-edited URL can't reach Firestore's `limit()` as NaN; `JokeContext.handleApiCall` lost its redundant guard and now reports a safe message for non-`Error` throws.
*   **Dead code (§1.12, commit f862225)**: removed `rateJoke` and `updateJokeCategory` (`jokeService`), `getUserRatingForJoke` (`ratingService`), their `JokeContext` entries, and `src/lib/constants.ts` (`SYSTEM_USER_ID`) — ~90 unreachable lines, zero callers each.
*   **Architecture (§2.1–§2.7)**: joke fetching moved to the pages with request sequencing; `fetchTopJokes` reimplemented against the Admin SDK; `/jokes` split into `src/lib/jokeFilters.ts` + `useJokeFilters` + `<JokeFilterDialog>`; shared `<CategoryCombobox>`; search tokenized to match stored keywords, with an honest empty state; stale-paint guard on client-side nav.
*   **Design/UX (§3.1–§3.7)**: Geist fonts actually wired up, fractional star fills, token-driven alerts, palette docs corrected, skeleton loading states, hardcoded fallback jokes removed, a11y fixes.
*   **Tests (§2.8, Task 9)**: Vitest widened to `src/**/*.test.{ts,tsx}` with `environment: 'jsdom'` (`jsdom` devDependency added; the `next/server` suites opt back into node with a `@vitest-environment` docblock). `parseCSVLine` extracted from `csv-import.tsx` into `src/lib/csv.ts` (behaviour unchanged; the "quoted field containing a newline" limitation is documented there and pinned by a test). New suites: `csv`, `jokeFilters`, `jokeService.buildJokesQuery`, `firestoreTimestamps`, `rateLimit`, and `verifyRequestAuth`. **26 → 146 tests**, all passing alongside lint and typecheck.
*   **Hygiene**: deleted `src/firebase-rules.txt` — a stale duplicate of the root `firestore.rules` predating round-1/3 rule work (no bounded rating-aggregate function, old ratings-delete rule) and referenced by nothing.

## DONE (2026-08-18, design round 4 — motion + visual system)
Executed via `docs/plans/2026-08-18-design-round-4.md` (18 tasks, one commit each, read-only review between each). **No behaviour changed**: no new fetches, no new state feeding a fetch, no changed conditionals. `npm run lint` ✅ · `npm run typecheck` ✅ · `npm test` **167/167 in 10 files** ✅ · `npx next build` ✅.

**Motion**
*   **Motion tokens (Task 1)**: `card-enter` and `shimmer` keyframes plus `ease-emphasized` (`cubic-bezier(0.22, 1, 0.36, 1)`, for things entering) and `ease-standard` (`cubic-bezier(0.4, 0, 0.2, 1)`, for state changes in place) in `tailwind.config.ts`. Every animation this round adds reads these.
*   **Reduced-motion policy (Task 2)**: one unlayered `@media (prefers-reduced-motion: reduce)` block at the end of `src/app/globals.css`, plus `motion-safe:`/`motion-reduce:` variants at the call sites. Deliberately *not* a blanket `* { animation: none }` — `animate-spin` keeps turning, because a frozen spinner reads as a hung request. Closes the WCAG 2.3.3 gap (the codebase previously had zero `prefers-reduced-motion` hits).
*   **Staggered card entrances (Tasks 3–5)**: `src/lib/motion.ts` (`nextStaggerBatch`/`entranceDelayMs`, 40ms step, capped at 12 cards) with its own test suite; a CSS mount animation on `JokeListItem`, so a "Load More" append animates only the appended page. The animation uses `backwards` fill, not `both` — a filled `to` keyframe would outrank the hover-lift and press transforms forever.
*   **Card and button feedback (Tasks 6–7)**: hover lift, press settle and `focus-visible:` (was `focus:`, which left a ring after a mouse click) on joke cards; `motion-safe:active:scale-[0.97]` in `buttonVariants`, so every button in the app acknowledges a press — the only feedback channel that exists on a phone.
*   **Star-rating preview (Task 8)**: `StarRating` fills to the hovered/focused value before you commit, and the hovered star grows. `RatingForm` lost `hover:text-primary/70`, which dimmed the one star under the cursor and read as "disabled". `onRatingChange` fires exactly when it did.
*   **Skeleton shimmer (Task 9)**: `.skeleton-bar` sweeps a highlight across each placeholder via `::after` instead of pulsing the whole card, so the card outline no longer throbs.
*   **Overlay timing (Task 10)**: `DialogOverlay` pinned to `duration-200` to match `DialogContent` (it was falling back to tailwindcss-animate's 150ms and finishing 50ms early on every close); `PopoverContent` given `duration-150`.
*   **Theme toggle (Task 11)**: `relative` added to the trigger — the absolutely positioned Moon icon was resolving against the sticky `<nav>` and only landed correctly because an all-`auto` absolute box stays at its static position. `transition-all` → `transition-transform` with an explicit duration and a reduced-motion opt-out. `disableTransitionOnChange` in `layout.tsx` is untouched on purpose.

**Visual system**
*   **Dark-mode surfaces (Task 12)**: `--card`/`--popover` `0 0% 13%` and `--muted` `0 0% 18%`, so dark mode has an actual card plane. They previously shared `0 0% 10%` with `--background`, and box-shadows are invisible on near-black, so every card was separated from the page by its border alone.
*   **One elevation scale (Task 13)**: in-flow card `shadow-sm` (`hover:shadow-md`), floating surface `shadow-lg`, focal card `shadow-md`. The skeleton card changed in the same commit as the real card, per the skeleton-parity constraint.
*   **One type scale (Task 14)**: page title / description / section title / card title / body, applied at call sites and documented in `src/app/globals.css`. A shared `<Header>` component now renders the page title on `/jokes`, `/add-joke` and `/manage`. No h2 is larger than an h1 any more.
*   **One page container (Task 15)**: `container mx-auto px-4 py-8 sm:px-6 md:py-12` across `/`, `/jokes`, `/joke/[jokeId]`, `/add-joke`, `/manage` (five containers were four different values). The home page reclaimed ~7rem above the fold — `header.mb-12 sm:mb-16` was stacked on the tagline's own `mb-16`.
*   **Mobile filter bar (Task 16)**: `flex-wrap` on the `/jokes` toolbar, the action cluster full-width below `sm`, and the `p-4`/`pb-6` conflict on the same edge resolved. At 375px the Filters button, the active-filter badges and "Log in to Add Jokes" each get their own row instead of being crushed into one.
*   **Shared `EmptyState` (Task 17)**: icon disc + headline + hint, used by `JokeList`, the home teaser and `CommunityRatings` (which were an icon, nothing, and a bare `<p>`). Icon + copy only — a CTA would be new behaviour.
*   **Batch-C review fixes (post-Task 17)**: `empty:hidden` on the `/jokes` badge row (its `basis-full min-h-[36px]` painted an empty 36px band on mobile with no filter active); `ring-1 ring-border` on the `EmptyState` disc; `role="status"` on the `EmptyState` wrapper; `hint` narrowed from `ReactNode` to `string`; the card easing sourced from the Task 1 token via `theme()` instead of a repeated curve.

**Contrast figures — correction to the Task 12 plan text.** The plan's verification note claimed `--muted-foreground` (`0 0% 63.9%`) at ≈8.0:1 on `--card` and ≈7.0:1 on `--muted` in dark mode. The real values are **≈6.4:1 on `--card`** (`0 0% 13%`) and **≈5.4:1 on `--muted`** (`0 0% 18%`). Both still clear WCAG AA for body text (4.5:1) and AA for large text; neither reaches AAA (7:1), which the plan's number implied.

**Accepted deferrals (round 4, decided up front)**
*   *Page-wide colour crossfade on theme change* — `ThemeProvider` sets `disableTransitionOnChange` (`src/app/layout.tsx:44`) to prevent a transition flash on hydration. Trading a first-paint flash for a 200ms crossfade is a bad deal; the round polished the toggle *icon* transition instead (Task 11).
*   *CTAs in empty states* — the scope was "icon + copy". A "Clear filters" button inside `JokeList` would be new behaviour, which the round's first hard constraint forbids.
*   *Removing framer-motion from `src/app/add-joke/page.tsx`* — the AI-variation cards keep their existing block. Removing it is not a polish-round change.

**Deferred to a possible round 5 (plan-level gaps found during the round, all real, none regressions)**
*   *Auth page `CardTitle` is still `text-2xl`* (`src/app/auth/page.tsx:90`) — the documented card-title scale is `text-lg font-semibold`. The auth card was in Task 13's file list for elevation but in no typography task.
*   *`add-joke-form.tsx` keeps its own `text-xs` label scale* (`:99`, `:117`, `:130`, `:148`) — the undocumented fifth scale named in audit item **V3**. Task 14 did not list the file, so it survived the round.
*   *`RatingForm`, `CommunityRatings` and `ExplanationCard` card titles are `text-xl`* (`:41`, `:28`, `:28`), against a documented `text-lg`. Same cause: elevation touched these files, typography did not.
*   *The auth page keeps a sixth container* — `container mx-auto flex justify-center items-center py-12 px-4` (`src/app/auth/page.tsx:87`), not the canonical `px-4 py-8 sm:px-6 md:py-12`. Task 15 listed five pages and `/auth` was not one of them; its centring wrapper needs a decision rather than a substitution.
*   *The joke-not-found card has no icon and no shape* (`src/app/joke/[jokeId]/page.tsx:333-338`) — audit item **V5** named four empty states and Task 17 fixed three. It should become an `EmptyState`.
*   *Dark-mode card/page separation is ≈1.09:1* (`--card` `0 0% 13%` on `--background` `0 0% 10%`). Task 12 made the plane exist, but the lightness step is weak on its own — `border-primary/20` is still carrying the separation. Raising `--card` to ~`0 0% 15–16%` would give a visible edge without shadows; it needs a look at both themes before it is changed, since `--muted` and the "used" card tint sit on top of it.

## DONE (2026-08-18, UI/UX round 5 — flows, feedback, IA)
Executed via `docs/plans/2026-08-18-ui-ux-round-5.md` (18 tasks, one commit each, read-only review between each). `npm run lint` ✅ · `npm run typecheck` ✅ · `npm test` **200/200 in 12 files** ✅ · `npx next build` ✅.

**This round changed behaviour, on purpose.** Round 4 was explicitly a no-behaviour-change round; round 5 is the opposite — it exists to fix flows that were wrong, not surfaces that were inconsistent. Every behaviour change, with the task that made it:

*   **A failed save keeps your text (Task 8, `6dd6171`)**: `handleAddJokeAndRedirect` no longer swallows the rejection from `addJoke`. `AddJokeForm` only calls `form.reset()` when the promise resolves, so a failed write used to clear the joke the user had just typed (or generated and then edited) and leave them with a red toast and an empty form. Now every field survives a failure.
*   **Category and permission errors are visible at all (Task 8, `6dd6171`)**: `handleApiCall` suppresses errors matching `Category name cannot be empty` and `permission denied`, on the stated assumption that "the callers surface them in context". No caller did — a category failure produced no user-visible output anywhere in the app. Both forms now set the message on the field it belongs to, and the edit form renders its root error for the first time.
*   **The delete confirmation survives the delete (Task 9, `7b7ea1a`)**: the `AlertDialog` is controlled and `AlertDialogAction` calls `preventDefault()`, so the dialog stays open across the request and stays open if it fails (retry in place). `isDeleting`/`Deleting…` were unreachable before — Radix unmounted the button on click. Escape, overlay click and Cancel are inert while a delete is in flight. Copy: `Are you sure you want to delete this joke?` → `Delete this joke?`, `Cancel` → `Keep it`, `Yes, delete joke` → `Delete joke`.
*   **One toast per action (Task 7, `6436ab2`)**: add, update, delete and rate each fired two toasts, and with `TOAST_LIMIT` at 1 the second evicted the first — so which message the user saw depended on call order. Errors are now announced by `handleApiCall` alone; successes by whichever layer holds the most specific copy (`handleApiCall`'s `successMessage: string` became `success: {title, description} | null`, where `null` means "the caller announces this one"). Titles are sentence case, ≤ 3 words, and destructive ones open with `Couldn't `. The rule is written into `src/hooks/use-toast.ts`, where the next author will look.
*   **Unreachable validation branches deleted or made reachable (Task 7, `6436ab2`; Task 16, `90f7289`)**: the "Please select a star rating (1-5)" toast could never fire (`RatingForm` disables submit at zero stars) and is gone. The auth page's two "Validation Error" toasts could never fire either, because `required`/`minLength` made the browser block the submit first — they are now inline field messages that actually appear.
*   **Active filters are individually removable (Task 10, `9e33343`)**: the six hand-written read-only badges became chips from `activeFilterChips`, each with an × that applies the filter set minus that one filter. Removing one filter is one click instead of reopening the dialog and hunting in a popover, and because it navigates through `applyFilters` it is undoable with the browser back button. "Clear All" is unchanged.
*   **Empty states offer a way out (Task 11, `a3eb22d`; Task 15, `5524a82`)**: `EmptyState` gained an `action` slot and `/jokes` offers "Clear filters" whenever anything is filtered, or "Try again" when the fetch failed. The unfiltered copy changed from "No jokes found. / Try adding some or adjusting your filters!" — which named filters that were not active — to "No jokes here yet. / Add the first one and it shows up right away."
*   **Search is a field on the page, and trimmed (Task 12, `56c205f`)**: it was a control inside the filter dialog, two interactions and one occluding surface deep, for the primary discovery path of a joke collection. It now applies on Enter or on the Search button, and the term is trimmed (the dialog applied it untrimmed). The dialog's search field is gone, along with the magnifier opener that was not a `DialogTrigger` — so Radix now returns focus to the Filters button, and the `setTimeout(…, 100)` focus race is gone.
*   **Scope is a toolbar toggle (Task 13, `edf93b9`)**: All jokes / My jokes is a visible two-button group on `/jokes` instead of a `<Select>` inside the dialog. It is the difference between "the app" and "my collection". The dialog no longer offers scope at all, and the "Login Required" toast that guarded its disabled option is deleted — the toggle is not rendered when signed out, because `useJokeFilters` downgrades `user` scope to `public` for a signed-out visitor anyway.
*   **Back from a joke returns to the feed you left (Task 14, `de367af`)**: the detail page's back button reads the last `/jokes` URL visited in this tab from `sessionStorage`, filters and all, instead of always `router.push('/jokes')`. It is a `<Link>` now, so middle-click and "open in new tab" work. Label: `Back to All Jokes` → `Back to jokes`. A deep link straight to a joke still gets `/jokes`, exactly as before.
*   **A failed feed fetch says so (Task 15, `5524a82`)**: `fetchJokesInternal`'s catch sets `jokes` to `[]`, which is indistinguishable from an empty result — so `/jokes` reported a network failure as "No jokes found. Try adjusting your filters", blaming the user's filters for a dropped connection. A new `jokesError` on `JokeContext` carries the reason; the feed renders the `WifiOff` face, the message and a working "Try again", and the home teaser gets the honest headline without a button (the feed is one click away). The destructive toast is unchanged.
*   **Auth validation moved inline (Task 16, `90f7289`)**: `required`/`minLength` replaced by `noValidate` plus our own checks, so the browser's native bubble is gone and the messages that were written are the messages that show, next to the field they belong to. Auth failures render as a `role="alert"` block instead of a destructive toast — a toast can be dismissed while the wrong password is still in the field. The password can be revealed, `autocomplete` is set on both fields (and switches between `current-password` and `new-password` with the mode), and errors clear as soon as the offending field is edited. The success toast stays.
*   **`/manage` is called Import (Task 17, `c9f12cb`)**: the nav label promised settings and delivered a file input — `Manage`/`Settings` → `Import`/`Upload`. The page title is "Import Jokes", and the "About This Page" card, which spent a whole Card explaining that this page is for bulk import, is one line with a link to `/add-joke`.

**Round-4 deferrals — all six closed**
*   **E1/E4 (Task 2, `eb76f7d`)**: the auth card *is* the page, so its title is now a real `<h1>` at the documented section-title step, and the centring became `flex justify-center items-start` on the canonical container. Six pages, one container — `items-start` because vertical centring fought the container's own `py-*` and pushed the card below the fold on a short phone viewport with the keyboard open.
*   **E2/E3 (Task 1, `6736653`)**: `RatingForm`, `CommunityRatings`, `ExplanationCard`, `csv-import` and four `edit-joke`/`add-joke`/`joke` titles all on `text-lg`. `add-joke-form`'s own `text-xs` label scale is gone, its nested `shadow-none border-0` Card is gone, and the `text-sm` overrides on its two inputs are gone — `Input`/`Textarea` are `text-base md:text-sm` precisely so mobile keeps 16px, and anything under that makes iOS Safari zoom the viewport on focus.
*   **E5 (Task 3, `02ca708`)**: the joke-not-found card is the fourth `EmptyState` — "We couldn't find that joke.", not the interjection "Hmm...".
*   **E6 (Task 4, `e0bccc9`)**: the dark card plane raised to a visible step (figures below).

**Dark-mode contrast after Task 4 — measured, not estimated** (sRGB relative luminance, computed). Round 4's plan overstated its numbers and had to be corrected after the fact; these were computed before the commit:
*   `--card` (`0 0% 16%`) on `--background` (`0 0% 10%`): **≈1.20:1**, up from ≈1.09:1 at 13%. A visible edge without the border doing the work.
*   `--muted-foreground` (`0 0% 63.9%`) on `--card`: **≈5.78:1** (was ≈6.4:1) — clears WCAG AA for body text.
*   `--muted-foreground` on `--muted` (`0 0% 20%`): **≈5.01:1** (was ≈5.4:1) — clears AA.
*   `--muted` on `--card`: **≈1.15:1** — a recessed block still reads.
Both foreground figures move *down* because the backgrounds got lighter; both stay above the 4.5:1 AA floor, which was the constraint. Ordering `background < card < muted < border` is preserved.

**New modules and their suites** (no React testing library in this repo, so logic is extracted into pure `src/lib/` modules and tested there — the same way rounds 3–4 met the obligation):
*   `src/lib/feedEmptyState.ts` + suite (Tasks 11, 15) — `describeEmptyFeed`, the four interacting conditions behind what the feed says when it shows nothing. The error branch outranks all the others.
*   `src/lib/feedReturn.ts` + suite (Task 14) — `rememberFeedUrl`/`readFeedUrl` over an injected `StorageLike`. Validated on both write *and* read: the value round-trips through storage where anything could have put it, so `//evil.example`, `https://evil.example/jokes` and `/jokes-evil` are all refused rather than handed to `<Link>`.
*   `activeFilterChips` in `src/lib/jokeFilters.ts` + cases in the existing suite (Task 10) — each chip carries the filter set that results from dropping exactly that one filter.
*   `src/components/PageLoading.tsx` (Task 5) — thirteen hand-rolled centred-spinner blocks in four layouts, and nine phrasings of "loading", became one component and five labels that each name what is loading. `animate-spin` deliberately keeps turning under `prefers-reduced-motion`, per the round-4 policy: a frozen spinner reads as a hung request.
*   `src/components/joke/BackToFeedButton.tsx` (Task 14) — the href starts at `/jokes` and is upgraded in an effect, because reading `sessionStorage` during render would disagree with the server and trip hydration.
*   **`EmptyState` gained an `action` slot (Task 11)**, deliberately reversing round 4's decision. Round 4 refused a CTA because it would have been a behaviour change and that round forbade them; round 5 allows them, and an empty result set caused by four active filters — or by a failed fetch — needs a way out that is not "work out what you did and undo it". One action, not a toolbar.

**Also in the round**
*   **Touch targets (Task 6, `4f28faf`)**: the toast close button is `opacity-100` below `sm` (there is no hover on a phone, and swipe-to-dismiss is undiscoverable) and keeps its hover-reveal from `sm` up. Interactive stars, the filter-dialog chip ×, "Clear All" and the detail-page Edit button all reach the WCAG 2.5.8 24×24px floor, most of them 36px.
*   **`…` everywhere (Task 5)**: literal `...` in user-visible strings replaced with the ellipsis character.
*   **`loadedFilters` untouched (Task 15)**: `git diff src/contexts/JokeContext.tsx` contains no changed line touching `loadedFilters` — including the `setLoadedFilters(filters)` in the `catch`, which is what stops a failed fetch painting skeletons forever. `jokesError` is a sibling field; the stale-paint guard on both pages is byte-identical.

**Accepted deferrals (round 5, decided up front)**
*   *Renaming `/manage` to `/import`* — Task 17 fixed the nav label, the page title and the filler card. Moving the route means a redirect plus every inbound link, for no additional user-visible gain.
*   *Removing `framer-motion` from `src/app/add-joke/page.tsx`* — third round in a row this is out of scope, and it still is. It stays confined to that one file.
*   *Optimistic updates for rate / toggle / delete* — the current pessimistic writes are *correct*: nothing paints until the write lands, and Task 15 makes failures legible. Optimism buys a few hundred milliseconds and costs a rollback path per action.
*   *An "unsaved changes" guard on the edit form* — it needs route-change interception the App Router does not offer cleanly, and Task 8 removed the case that actually lost data.
*   *A skip-to-content link* — real, but the nav is five controls deep at most. It belongs in an accessibility-focused round with a heading-order and landmark pass.

## DONE (2026-08-19, accessibility round 6 — WCAG 2.2 AA)
Executed via `docs/plans/2026-08-19-a11y-round-6.md` (16 tasks, one commit each, read-only review between each, plus two errata commits). `npm run lint` ✅ · `npm run typecheck` ✅ · `npm test` **249/249 in 15 files** ✅ · `npx next build` ✅.

**This is the first round that audited the app for accessibility.** Rounds 3–5 fixed structure, motion, visuals and flows, and each left the same gaps: no skip link, no page outline below the `h1` (every card title was a `<div>`), one document title for seven routes (so Next's own route announcer had never fired), a five-button star widget that never said what the rating was, "Load More" appending ten cards in silence, filter chips dropping focus to `<body>`, and error text at 3.4:1 light / 3.0:1 dark.

**Behaviour changes, each with its task and commit**
*   **A skip link is the first tab stop on every page (Task 1, `bde8db6`)**: `sr-only` until focused, then a visible ring at the top-left. It targets `<main id="main-content" tabIndex={-1}>` — the landmark had no `id` and no `tabIndex`, so there was nothing to skip *to*. Nothing moves for a mouse user. Both `<nav>` elements gained `aria-label="Main"`.
*   **Nav controls keep their names on a phone (Task 2, `1a8c011`)**: `hidden sm:inline` → `sr-only sm:not-sr-only` on the three link labels, the account menu and Login. `display:none` removes text from the accessible name and lucide stamps `aria-hidden` on the icons, so below 640px the primary navigation was five buttons with no accessible name at all. Layout above `sm` is unchanged.
*   **One title per route, which turns the existing route announcer on (Task 3, `e91290b`)**: a `title.template` of `'%s · Joke Hub'` in the root layout plus six segment layouts that carry nothing but `metadata` (`Browse jokes`, `Add a joke`, `Import jokes`, `Log in`, `Joke`, `Edit joke`). Next's App Router announcer speaks `document.title` on each client-side navigation and skips it when the string has not changed; with one static title across seven routes it had never announced anything. The browser tab now names the page too.
*   **Card titles that are page sections are real headings (Task 4, `1a0e357`)**: `CardTitle` gained an `as` prop (`div` still the default) and `EmptyState` a `titleAs`; ten `div`s became `h2`/`h1`. `/joke/{id}` went from one heading to four; `/add-joke`'s `h1 → h3` skip is gone. No class changed on any element, so nothing moved.
*   **The home page has an `h1` (Task 5, `503c31e`)**: the wordmark *is* the page title, so it became the heading rather than gaining a second one, and `Logo`'s `alt` went from "Joke Hub Logo" to "Joke Hub" — "logo" is read aloud and says nothing. `mb-5` moved with the element; the wordmark is pixel-identical.
*   **Destructive text and dark-mode links reach 4.5:1 (Task 6, `df7f285`, with `7123c35`)**: `--destructive` is now a *surface* token only and a new `--error` carries destructive *text* (form messages, error blocks, the sign-out item, both auth field errors). Visible consequence: the destructive button and toast are a deeper red in light mode, error text is darker in light and lighter in dark, and dark-mode purple is one step brighter. Figures below.
*   **Root form errors are announced, required fields say so, the slider has a name (Task 7, `a821b3e`)**: the root `<FormMessage>` in `add-joke-form` and the edit page read a `FormFieldContext` that does not exist out there — they rendered with `id="undefined-form-item-message"`, referenced by nothing and with no role, so "You do not have permission to add this joke." was announced by nothing. They are `role="alert"` paragraphs now. `aria-required` on the two required controls in both forms; the comment counter is wired to its textarea with `aria-describedby`; `Slider` gained a `thumbLabel` because Radix puts the root's `id` on the wrapper span while `role="slider"` sits on the thumb, which had no name.
*   **Toast sensitivity follows the variant (Task 8, `e1bb649`, corrected in `af11636`)**: Radix defaults every toast to `type="foreground"`, i.e. `aria-live="assertive"`, so "Joke added" cut off whatever the screen reader was saying. The rule is now **destructive → `foreground` (assertive), everything else → `background` (polite)**, set in `Toaster` *after* the prop spread so a call site cannot quietly override it. The close button gained an `sr-only` "Dismiss" (safe: Radix wraps `Close` in `ToastAnnounceExclude`, so it is not read as part of the toast) and a token-coloured focus ring instead of Tailwind's off-palette blue default. This rule belongs with the other toast conventions in `src/hooks/use-toast.ts:11-26`, which does not yet carry it — round 6's Task 16 was scoped to this file only.
*   **The AI panel honours `prefers-reduced-motion` (Task 9, `d4563f4`)**: `MotionConfig reducedMotion="user"` around the `/add-joke` tree. Round 4's policy is a CSS media query and cannot reach animations framer-motion drives in JS, so the generation panel had been sliding 20px per card for reduced-motion users for three rounds. With the setting on, the cards cross-fade; everyone else sees what they saw before.
*   **Long operations say they are running (Task 10, `04ce6a8`, corrected in `af11636`)**: `role="status"` lines for AI generation ("Generating witty humor…"), the explanation stream ("Writing the explanation…" → "The explanation is ready.") and the in-flight delete ("Deleting the joke…"). Nothing visible changed. The streamed paragraph itself is deliberately *not* a live region — it would be announced a few words at a time.
*   **The CSV import reports progress and its result on the page (Task 11, `ca29c73`)**: the outcome existed only in a toast that interrupted once and then had to be dismissed. It is now also a permanently mounted `role="status"` line under the file input, which sighted users can read back against their file. Copy: `5 joke(s) imported.` → `5 jokes imported.` / `1 joke imported.`, and the all-skipped sentence pluralizes the same way.
*   **"Load More" announces what arrived (Task 12, `43ff6e5`)**: `10 more jokes loaded. 20 now showing.`, politely, visually hidden. Deliberately **announce, do not move focus** — the button the user pressed is where they want to stay, and the grid is not a live region because that would read ten whole joke cards aloud.
*   **Removing a filter chip hands focus on (Task 13, `8d83514`)**: focus goes to the chip that takes the removed one's place, or the one before it if it was last, or the Filters button when the row empties. "Clear All" hands focus to the Filters button, because it unmounts itself along with the chips. Previously each removal dropped focus to `<body>`, so clearing four filters meant four trips back through the page. `JokeFilterDialog` accepts a `triggerRef` for this; removal still goes through `applyFilters`, so the back button still undoes it.
*   **The star rating is one radio group with a value (Task 14, `4f24d11`)**: `role="radiogroup"` with a name, `role="radio"` + `aria-checked` per star, and a roving tabindex — **one tab stop instead of five**, with Left/Right/Up/Down/Home/End setting the value (standard radio-group behaviour; it submits nothing, and Submit is unchanged). Each option is labelled with its value ("3 stars") instead of "Set rating to 3 stars", so the group's name carries the verb. Hint copy: "Click a star to rate." → "Choose a star to rate, or use the arrow keys." — the old copy named the one input method that is no longer the only one. Clicking and hover preview are unchanged, and `onRatingChange` still fires once per chosen value. Joke cards now pass a `label` that includes the rating count, which previously existed only in a tooltip on a non-focusable `<div>`.
*   **A route change moves focus into the page (Task 15, `14456e2`)**: `RouteFocus` focuses `<main>` on each `pathname` change, with `preventScroll` so the router's own scroll restoration (including Back) still stands. Not on the first render — the browser has already put focus where it belongs. Keyed on `pathname` only, deliberately: `/jokes` re-navigates to itself on every filter change, and pulling focus out of the toolbar is exactly the bug Task 13 fixes. This is also what covers joke-to-joke navigation, which announces nothing (see the deferrals).

**Contrast figures — computed sRGB relative luminance, not estimated**
*   Light `--destructive` `0 84.2% 60.2%` → **`0 72% 45%`**: `--destructive-foreground` (98%) on it goes **3.76:1 → 5.81:1**, which fixes the "Delete Joke" label and every destructive toast. Dark `--destructive` is unchanged at `0 62.8% 50.6%` (white label 5.02:1, already passing).
*   New `--error`, destructive *text* only: light **`0 72% 42%`** — **6.47:1** on `--background`, **5.47:1** on `bg-error/10`. Dark **`0 90% 75%`** — **6.19:1** on `--card`, **7.30:1** on `--background`, **4.92:1** on `bg-error/10`. A surface colour and a text colour cannot be one token: in dark mode they need opposite luminances (the surface value reads 2.95:1 as text on `--card`).
*   Dark `--primary` `270 60% 65%` → **`270 60% 72%`**: in-card `text-primary` links **4.22:1 → 5.51:1**, and inside a `bg-primary/10` alert **4.14:1 → 4.67:1**. The plan's 70% was corrected during the round (`7123c35`): it left the tinted-alert composite at **4.36:1**, still under the floor, because the tint lightens the surface the text sits on — the alert, not the plain card, is always the binding case. `--primary-foreground` (10%) on 72% stays above 6:1, so the filled button is unaffected, and `--ring` deliberately stays at 65% (a focus indicator needs 3:1 and has 4.98:1).

**New modules and their suites** (still no React testing library in this repo, and this round did not add one — logic goes into pure `src/lib/` modules and is tested there, the way rounds 3–5 met the same obligation)
*   `src/lib/csvReport.ts` + suite (Task 11) — `summarizeImport`/`describeSkippedRows`: the four shapes an import outcome comes in, and the row list (`rows 4, 19, 27 and 3 more (…)`, ten numbers then a summary, reasons de-duplicated). The same sentence goes to the toast and to the page's status region, so it is built once.
*   `src/lib/feedAnnounce.ts` + suite (Task 12) — `describeFeedAppend`. Encodes why an append **cannot be detected from a count alone**: applying a filter also changes the number of cards, so a count is only comparable against one taken for the *same* filter set, and the serialized filter key travels with it. Empty for a first snapshot, a key change, the first page of a set, and anything that did not grow.
*   `src/lib/starRating.ts` + suite (Task 14) — `ratingFromKey` (both axes wired, wrapping per the APG, `null` for keys the widget does not handle) and `tabbableStarIndex`, which guards a fractional or out-of-range rating that could otherwise leave the group with **no** tab stop at all.
*   `nextChipFocusKey` in `src/lib/jokeFilters.ts` + cases in the existing suite (Task 13) — takes the chip row *as it stands before the removal*, because the answer has to be computed in the click handler, before the navigation unmounts the button that has focus.
*   `src/components/RouteFocus.tsx` (Task 15) — a render-nothing client component; the `pathname`-only dependency is the whole design (see above).

**Audited and found correct — so the next round does not re-audit it**
*   **Touch targets**: the 24px WCAG 2.5.8 floor from round 5 holds app-wide — chip × 24px, toast close 24px, interactive stars 36px, every `size="sm"` button 36px, `Switch` 24×44, `CommandItem`/`SelectItem` ~32px, the auth reveal button 40px. The only sub-24px target is the `Slider` thumb at 20px, which **passes on the spacing exception**: no other target's 24px circle intersects it.
*   **Dialog focus return** is correct after round 5: the single opener is a real `DialogTrigger`, so Radix restores focus to the Filters button; the `AlertDialog` trigger likewise.
*   `html lang="en"` is set. **Colour is never the only cue** (field errors are text, the error cards pair the tint with a `ShieldAlert`, the scope toggle pairs colour with `aria-pressed` and a filled-vs-outline treatment) — 1.4.1 passes.
*   `--muted-foreground` 5.74:1 light / 5.78:1 on dark `--card` / 5.01:1 on dark `--muted`; `--accent-foreground` on `--accent` 5.45:1 light, 5.61:1 dark; focus ring `--ring` 6.45:1 light and 4.98:1 dark against the page, needing 3:1. Round 5's figures re-verified.
*   **`loadedFilters` untouched (Tasks 12 and 13)**: both added state next to the stale-paint guard on `/jokes`; the diff contains no changed line touching `loadedFilters`, and no `setLoadedFilters` call or consumer comparison changed meaning. `firestore.rules` and the four API routes were not opened this round — nothing here writes anything new.

**Errata inside the round** (both found by review, both fixed in-round)
*   **`7123c35`** — dark `--primary` to 72%, not the plan's 70% (figures above); a `focus:ring-offset-background` added to the skip link, whose offset ring was otherwise resolving against an unstated background; and `EmptyState`'s title tag hoisted out of the IIFE the plan wrote into the JSX.
*   **`af11636`** — three live-region errata. `aria-busy` moved off `AlertDialogContent` onto the action button: on an ancestor it covers the status paragraph, and a live region inside a busy subtree is held back until busy clears, which for a successful delete never happens because the dialog unmounts. The generation status became a permanently mounted `sr-only` line instead of `role="status"` on the element that mounts with the message already inside it — **a live region that mounts with its text does not announce**, which is the rule the whole round follows. And `type=` moved *after* `{...props}` in `Toaster`, so the sensitivity rule is authoritative rather than overridable from a call site.

**Accepted deferrals (round 6, decided up front)**
*   *The dangling `aria-describedby` in `src/components/ui/form.tsx:116-120`* — `FormControl` always references `${id}-form-item-description` and most fields render no `<FormDescription>`. Per accname an unresolvable idref is ignored, so nothing is mis-announced and the message id still resolves on error; only axe's `aria-valid-attr-value` rule objects. Fixing it properly needs `FormItem` to know whether a description was rendered *before* `FormControl` renders — a two-pass registration for zero user-facing gain.
*   *A per-joke title on `/joke/[jokeId]`* — Task 3 gives the segment the static "Joke". A real one needs `generateMetadata` reading Firestore through the Admin SDK on a route that is currently client-fetched only: a data-layer change, not an a11y change. Stated consequence: joke → joke navigation announces nothing, which is why Task 15 exists.
*   *A page-level `h1` on the "joke not found" branch* — it renders an `EmptyState` whose title is an `h1`, and the surrounding page has no `Header`. One heading on the page, at level 1: correct, if unusual.
*   *Moving the home page footer into `layout.tsx`* to make it a real `contentinfo` landmark — it would put a copyright line on all six other routes in exchange for one landmark on one page.
*   *A visible "required" marker on form labels* — Task 7 adds `aria-required`; the visible convention here is to mark the *optional* field, which is unambiguous while exactly one field is optional.
*   *Removing `framer-motion` from `src/app/add-joke/page.tsx`* — fourth round running. Task 9 made what is there honour reduced motion and left it where it is.

## DONE (2026-08-19, UI/UX round 7 — content, scale, sharing, weight)
Executed via `docs/plans/2026-08-19-ui-ux-round-7.md` (16 tasks, one commit each, read-only review after each batch, plus one errata commit). `npm run lint` ✅ · `npm run typecheck` ✅ · `npm test` **282/282 in 16 files** ✅ · `npx next build` ✅ (12 routes).

**This round is about the content itself, and the app at 200 jokes.** Six rounds of structure, motion, visuals, flows and accessibility never looked at what a joke *is*: a text a user wrote with the punchline on its own line, which every surface in the app rendered as one run-on paragraph while the AI's explanations and other people's comments kept their shape. It also closes three of round 6's recorded leftovers (the inert `role="status"` blocks, the toast sensitivity rule missing from the conventions block, the comma in a category name) and two filters that could not do what their labels said.

**Behaviour changes, each with its task and commit**
*   **A joke keeps its line breaks, and one long joke stops stretching its row (Task 3, `9c93d6e`)**: `whitespace-pre-line` on the card text, the detail-page `h1` and the AI variation cards — the three surfaces that render `joke.text`. `line-clamp-6` on the card only (a CSS grid row is as tall as its tallest card, so a 400-word bit left up to three neighbours as mostly whitespace; the full text is one click away). `break-words`, because a URL with no spaces in it ran past the content box. The category badge moved from `absolute bottom-0 left-0` into the flow with `mt-auto` and a truncating span, so a long name ends in an ellipsis instead of being clipped mid-word by the card's `overflow-hidden` — and the `pb-8` that reserved room for the absolute box went with it. `text-[11px]` → `text-xs`: the last arbitrary font size in `src/`.
*   **The card states its rating and says when a joke is used (Task 4, `e3e68da`)**: the average and count are text in the footer (`4.2 (7)`), not a `TooltipContent` on a non-focusable `<div>` that no keyboard and no touch user could ever reach — and the per-card `TooltipProvider` is gone with it (twelve per page, two hundred in a paged-in collection). The owner's "used" flag is a `✓ Used` badge instead of a `bg-muted/30` tint measuring 1.03:1 (figures below). Community comments are signed "A user", the label the detail-page byline already uses, instead of the literal `User` with a comment asking for a real identifier.
*   **A failed import reports once and keeps its partial count (Task 6, `03fe427`)**: one boolean (`writeStarted`) separates "the file is bad" from "the write failed". A parse failure is still one destructive toast. A *write* failure no longer fires a second toast that evicted `handleApiCall`'s (with `TOAST_LIMIT` at 1) and blamed the user's file for a permission or network problem; the page's status line carries the detail instead, including `jokeService.importJokes`'s partial count ("Imported 40 of 120 jokes before the import failed: …"), which says what state the collection is now in and exists nowhere else in the UI. It used to be wiped by `setStatusMessage('')` on the next line.
*   **A joke can be shared (Task 7, `d37ca06`)**: a "Copy link" button on `/joke/{id}`, for signed-in and signed-out visitors alike, writing `https://<origin>/joke/<id>` — the clean URL, even when you arrived from `/jokes?scope=user&funnyRate=5`. The label flips to "Copied" for two seconds *and* a polite toast fires: the label is the feedback for the person looking at the button, the toast is the one that gets announced.
*   **The account email stops crowding the nav (Task 8, `540e6f8`)**: `sr-only lg:not-sr-only` on the account trigger's label, not `sm:` like the three nav links — the visible string there is an email address, which at 640–768px shared one row with the wordmark, three icon-and-label links and the theme toggle. The name stays in the accessibility tree at every width (that is what round 6 fixed and this does not undo), truncated at `16ch` once visible, and the full address is the first item in the menu it opens.
*   **"Add a joke" opens before your categories land (Task 9, `cb33e8c`)**: the page-level `loadingCategories` gate is gone, and it is out of `AddJokeForm`'s blanket `isFormDisabled` — it used to disable the **joke text field**, so a user with 80 categories on a slow connection could not start typing their joke because a combobox was loading. `CategoryCombobox` already disables itself while it loads, which is the right scope; the submit button is gated on the same flag, because a joke cannot be saved before its category picker works. `/manage` is untouched — its gate has a stated reason.
*   **Dialogs scroll on a short viewport (Task 10, `c611c23`)**: `max-h-[calc(100dvh-2rem)] overflow-y-auto` on `DialogContent` and `AlertDialogContent`. Both were `fixed top-[50%] translate-y-[-50%] grid` with no height bound, so on a phone in landscape the filter dialog's footer was off-screen with nothing to scroll and **Apply Filters was unreachable** — a dead end, not a polish item. `dvh` and not `vh` because mobile Safari's `vh` counts retracted browser chrome, which is the case this is for. Nothing changes on a viewport tall enough for the content.
*   **The feed announces its result (Task 11, `d93f782`)**: `EmptyState`, `PageLoading` and `JokeGridSkeleton` each mounted `role="status"` with the message already inside — and **a live region that mounts with its text does not announce** (round 6's rule, and its `af11636` erratum). So searching for a word that matched nothing produced total silence. `/jokes` now has one permanently mounted region whose *text changes*: "Loading jokes…", "12 jokes shown.", "10 more jokes loaded. 22 now showing.", or the empty-state sentence word-for-word as it appears on screen. The two inert roles on that page came off; the round-6 append announcement fires identically.
*   **The feed says how many jokes there are, and pages by twelve (Task 12, `49d2cb2`)**: a visible tally above the Load More button — `Showing 24 jokes so far.` / `Showing all 31 jokes.` — replacing "No more jokes to load for the current filters.", which reported the *absence* of more jokes and never the size of what was there. `PAGE_SIZE` 10 → 12: the grid is 1/2/3/4 columns, so ten left a ragged last row at every breakpoint above one (4+4+2, 3+3+3+1) while twelve divides evenly by all four, and a 200-joke collection costs seventeen "Load More" presses instead of twenty. Every explicit-`limit` caller is unaffected, so the home teaser still shows exactly three.
*   **A category survives a comma in its name (Task 13, `a6d5f4e`)**: `selectedCategories` round-trips through one repeated `categories` param per category (`?categories=Puns&categories=Dad`) instead of one comma-joined value, with `getAll` on the read surface. A category called `Dad, jokes` — a name a user will type, because it reads as prose — used to parse back as two names, neither of which existed, so both were pruned: the chip vanished from the toolbar and the feed answered a query nobody asked for. Values are trimmed and de-duplicated, so a hand-edited URL cannot produce the same chip twice. **Stated casualty:** a bookmarked URL in the old comma form with more than one category now resolves to a single category literally named `A,B`, which matches nothing. Multi-category links were the only ones affected, and a link carrying a comma-containing name was already broken — that is the bug being fixed.
*   **The filter dialog only offers filters that can work (Task 14, `035910e`)**: the Categories row is hidden when there is nothing to choose (signed out, or signed in before the first joke, it rendered a permanently disabled combobox reading "Select categories…" with "No categories available." behind it, in the top slot). Usage Status is signed-in only — `used` is the owner's own bookkeeping, and a signed-out visitor filtering the public feed by it is filtering strangers' private notes. "Rating" became **"Own rating"** in the dialog and on the chip, with a one-line hint: it filters `funnyRate`, the score a joke's *author* gave it, while the stars on every card are the *community* average, so the old label read as a filter on the number the user was looking at. No filter's behaviour changed and the URL format is untouched, so a deep link with `funnyRate` or `usageStatus` still applies — which is what keeps the signed-out downgrade honest.
*   **Your own rating is something you can set (Task 15, `0c54160`)**: `funnyRate` was filterable, was one of the filters in the dialog, was written by the CSV import and by `/api/jokes/add`, and **no screen in the app could set it** — so for every joke added through the UI it was `0` forever and "Own rating: 4 Stars" returned an empty feed. The edit form now carries a star rating (the same `StarRating` widget as the community rating: one radio group, one tab stop, arrow keys, round 6's Task 14 semantics unchanged), a "Clear rating" link back to Unrated (a radio group has no unselect gesture, and "Unrated" is a value the filter offers), and `funnyRate` in the "nothing changed" comparison — without which a rating-only edit reported "Nothing to save" and navigated away, discarding it. `firestore.rules` needed no change: the create rule already validates `funnyRate` in 0–5 and the owner branch of `canUpdateJoke` already permits the field. The add form stays three fields.
*   **Bytes on every page load (Task 1, `f0e4fcc`)**: the browser-tab icon is a ~300-byte purple "J" mark instead of the 209KB wordmark, and `Geist_Mono` is gone from the root layout. The `/add-joke` temperature readout renders in Geist with `tabular-nums` and still does not reflow as the slider moves.
*   **`CardTitle` defaults to the documented scale (Task 2, `6d2137e`)**: `text-2xl` → `text-lg font-semibold`, and the `text-lg` every one of the eleven call sites passed to undo it is gone. **No rendered title changed size**; a *new* card gets the documented one, from the component rather than from a reviewer's memory. The unsound `forwardRef<HTMLHeadingElement>` on a component that renders a `div` by default is now `HTMLElement`, which is honest for all four tags — same six lines, same commit.
*   **The toast rules say everything they enforce (Task 5, `b225cfa`)**: round 6's sensitivity rule (destructive → assertive, everything else → polite, decided in `Toaster` after the prop spread so a call site cannot override it) is now in the conventions block at `use-toast.ts:11-26` with the rest of the contract. `csvReport`'s five-word title `Import finished with skipped rows` became `Some rows skipped`, and the exception argued in its own JSDoc is deleted rather than documented — a new test pins every title in the module at three words or fewer.

**The measured figures — computed, not estimated**
*   The `bg-muted/30` used-joke tint: light `--muted` (96.1%) at 30% over a white card composites to 98.83% — **1.03:1** against the card. Dark `--muted` (20%) at 30% over a 16% card composites to 17.2% — **1.04:1**. The feed-level signal for the flag that the entire `usageStatus` filter exists to track did not exist; it is a text badge now (Task 4). It was also the one place left in the app where a state was signalled by colour alone, which round 6's 1.4.1 pass missed.
*   The favicon: `/logo.png` is **209,273 bytes**, and a `<link rel="icon">` is fetched raw, so the image optimizer never saw it — 209KB on every page load to render a 1395×312 wordmark into a 16px square where it was an illegible sliver. `public/favicon.svg` is **290 bytes** and sharp at every size.
*   `font-mono` appeared at **exactly one call site** in the app (the `/add-joke` temperature readout) and cost a whole `Geist_Mono` webfont family on all seven routes. `grep -rn 'font-mono' src/` is now 0, as is `grep -rn 'text-\['`.

**New modules and their suites** (still no React testing library in this repo, and this round did not add one — logic goes into pure `src/lib/` modules and is tested there, the way rounds 3–6 met the same obligation; `src/lib/` is outside Tailwind's content globs, so nothing there returns a class name)
*   `src/lib/share.ts` + suite (Task 7) — `jokeShareUrl` builds the canonical URL from the **origin and the id**, not from `location.href`, so a link copied off a filtered or otherwise decorated URL is still the clean one; the origin's trailing slashes are stripped, because `https://host/` + `/joke/x` is `//joke/x`. `copyToClipboard` reports success as a **boolean rather than throwing**: `navigator.clipboard` is absent on an insecure origin and rejects when the document is not focused, the caller shows a toast either way, and "there is no clipboard here at all" is the same answer as "the write failed".
*   `describeFeedStatus` in `src/lib/feedAnnounce.ts` + suite (Task 11) — four things one region has to say, in priority order: a fetch is running, the result is empty (the region repeats the on-screen sentence verbatim, so the two cannot contradict each other), a page was appended, or this is a result set with its size. It **delegates to `describeFeedAppend` rather than replacing it**: that function's rules — a filter change also changes the count and is not an append, and a first page is not "more" — are the hard part and are already pinned by round 6's tests. It never returns `''`, because a permanently mounted region only announces text that *changes*.
*   `describeFeedTally` in the same module + suite (Task 12) — the visible counterpart, `''` for an empty set because the empty state is already saying everything there is to say.

**Audited and found correct — so the next round does not re-audit it**
*   **The `/jokes` toolbar wrap at 375px is correct** after round 4's Task 16: search takes its own row (`basis-full`), the scope toggle and Filters share the next, the chip row takes one (`empty:hidden` when there are none), and the action cluster is `w-full`. No fix.
*   **`next/image` usage is correct**: one image in the app (`logo.tsx`), fixed width, `priority` on the nav instance only. The `remotePatterns` entry for `picsum.photos` in `next.config.ts` is dead but harmless.
*   **`PageLoading` keeps a `role="status"` that does not announce, deliberately** — it is as inert as the two that came off, but it appears on a *navigation*, which round 6's per-route titles already announce through Next's route announcer. The reason is now a comment in the file so it is not re-derived.
*   **`src/app/favicon.ico` (34,754 bytes) is kept on purpose** — Next's App Router serves it at `/favicon.ico`, which is the fallback for browsers that do not take an SVG icon (Safari before 16.4 among them). Task 1 replaced the `icons.icon` *link target*, not the conventional file. Reviewed in batch 1; decision: keep.
*   **`dvh` degrades safely**: on iOS below 15.4 the `max-height` declaration is invalid and dropped, which restores the pre-Task-10 behaviour. No fallback needed. `overflow-y-auto` also makes `overflow-x` compute to `auto`, but every nested overlay in a dialog (`popover`, `select`, `dropdown-menu`) renders in a Portal, so the category picker inside the filter dialog is not clipped.
*   **`src/components/ui/tooltip.tsx` is now orphaned** — Task 4 removed the app's only consumer. It is an unmodified shadcn primitive; deleting it is a decision about the component library, not about this round. No action.
*   `text-[…]` font sizes are **zero** across `src/`, closing the fifth type scale round 4 set out to delete.

**Errata and judgment calls inside the round**
*   **`86bc916`** — comment prose declassified so the arbitrary-font-size tripwire (`grep -rn 'text-\[' src/`) reads clean: a comment quoting the class it deleted kept the grep at 1. The reasoning is unchanged, only the literal token is paraphrased. This became the round's **precedent**, applied again in Tasks 9, 11 and 12 (`No category-loading gate here`, `No status role and no aria-busy here`, `a status region that mounts with its message`, and Task 12's end-of-list sentence) so each task's own verification grep reads the value the plan states.
*   **Two verification greps in the plan state pre-change values**, proved wrong by the code being right: Task 8's `sm:not-sr-only` count is 2 after the change, not 3 (the step converts one of the three to `lg:`), and Task 11's `loadedFilters` count of 1 in `git diff` is a `-U3` context line, not an edit.
*   **Task 14's `modal-funny-rate-hint` id is referenced by nothing.** The plan prescribes it verbatim, and the hint is visible text that reads fine; wiring it would mean `aria-describedby` on a Radix `SelectTrigger`, which is a change the plan does not ask for. Recorded rather than improvised.
*   **Task 15's `FormLabel` `for` attribute is dangling**, because `StarRating` does not accept an `id` — the widget's accessible name comes from its own `label` prop ("Your own rating for this joke"), exactly as in `RatingForm`. `FormControl` attaches no ref to it (Radix `Slot` only composes a ref when one was forwarded), so nothing warns and nothing is mis-announced.

**Known behaviour recorded, not fixed (found by the batch-2 review)**
*   **The first "Loading jokes…" on `/jokes` is silent.** While `authLoading` is true the page early-returns `PageLoading`, so the status region mounts already carrying the text. This is what Task 11 states ("on every reload *after* the first"), and every user-caused reload announces correctly.
*   **A "Load More" that appends zero jokes announces nothing** — the count does not change, so the region keeps its text. Round 6 was equally silent here; Task 12's visible tally is the practical answer.
*   **The empty-state *hint* is announced by nothing.** The region speaks `emptyCopy.title` only, and round 4's comment on the removed wrapper role claimed the headline and the hint were read "as one message" — which they never were, because that role never announced at all. `${title} ${hint}` is a candidate for a later round.
*   **A failed initial fetch produces two announcements**: `JokeContext`'s destructive toast ("Couldn't load jokes.", assertive) and the region ("We couldn't load the jokes.", polite). Both are true and their sensitivities differ deliberately.
*   **The `/joke/{id}` not-found branch announces nothing.** Task 11 removed the inert `role="status"` from `EmptyState` app-wide, and the plan's deferral named the home teaser and the community list but not this third site. Not a regression — the removed role was equally inert — but Task 7 makes a shared link to a deleted joke a real path, so the site is recorded here. Giving that branch a permanently mounted region is the fix if it is wanted.
*   **`CopyLinkButton`'s two-second timer has no cleanup and no reset**, so a second press inside the window does not restart the label, and an unmount inside it calls `setJustCopied` on a dead component (a no-op in React 18, no warning). One `useRef` for the handle fixes both. There is also no `document.execCommand` fallback on an insecure origin — the destructive toast is the whole answer, which is honest, and `execCommand` is deprecated. No change recommended.
*   **The share control is visually quieter than Back** (`ghost` against `outline`, same row) on the page whose new purpose is sharing, and `navbar.tsx`'s `UserCircle` keeps `sm:mr-2` while its label is now visible only from `lg`, so 640–1023px carries 8px of trailing space after the icon. Both plan-prescribed; both a design call for a later round. `DialogPrimitive.Close` is `absolute right-4 top-4`, so on a short scrolling viewport the X leaves the screen — Escape and the footer buttons still work, so it is not a dead end; `sticky` is the fix if it is ever wanted.

**Accepted deferrals (round 7, decided up front)**
*   *Removing `framer-motion` from `src/app/add-joke/page.tsx`* — **fifth round running**. The brief allowed it back in scope only on a concrete user-facing problem. The candidate was bundle weight on the route where you type a joke, and this audit **could not measure it**: Next 16 with Turbopack prints no per-route size table (verified against `npx next build`). An unmeasured suspicion is not a concrete problem.
*   *Renaming `/manage` to `/import`* — third round. The nav label, the page title and the filler card were all fixed in round 5; the route rename is a redirect plus every inbound link for no additional user-visible gain.
*   *Optimistic updates for rate / toggle / delete* — the pessimistic writes are correct and round 5's Task 15 made their failures legible. Unchanged reasoning.
*   *An "unsaved changes" guard on the edit form* — still needs route-change interception the App Router does not offer cleanly, and round 5's Task 8 removed the case that actually lost data.
*   *A per-joke `<title>` on `/joke/[jokeId]`* — round 6's deferral stands: it needs `generateMetadata` reading Firestore through the Admin SDK on a client-fetched route. Task 7 gives that page a shareable link, which is the user-facing half of the same want.
*   *Repositioning dialogs for the on-screen keyboard* — Task 10 fixes the short *layout* viewport. Following the *visual* viewport as the keyboard opens needs a `window.visualViewport` resize listener and a positioning override on a Radix-owned element: a new mechanism, not a polish fix. The filter dialog's only keyboard-summoning control is the category search inside a popover, which Radix flips on collision.
*   *Live regions for the home teaser and the community-ratings list* — Task 11 gives `/jokes` a real one because that is where results vary by user action. The other `EmptyState` sites resolve once per page load, where round 6's route-title announcer already reports the navigation.
*   *Display names for raters* — Task 4 changes "User" to "A user", which is honest. Real attribution needs a profile field, an auth-flow change and a privacy decision about publishing anyone's identity next to their opinion of a joke.
*   *A `funnyRate` field on the **add** form* — Task 15 puts it where it belongs: you rate your own material after living with it, not in the same keystroke as writing it.

## DONE (2026-08-19, improvement round 8 — the routes nobody visits on purpose)
Executed via `docs/plans/2026-08-19-round-8.md` (16 tasks, one commit each, read-only review after each batch, plus three errata commits and one records commit). `npm run lint` ✅ · `npm run typecheck` ✅ · `npm test` **305/305 in 18 files** ✅ · `npx next build` ✅ (**14 routes**).

**This round covers the routes a user reaches by accident or from outside.** Seven rounds polished the paths walked on purpose — the feed, the joke page, the forms, the dialog. Nothing had ever looked at a link pasted into a chat window, a crashed render, a URL that no longer resolves, a crawler or a printer: the app had **no error boundary anywhere**, no `not-found.tsx`, no `metadataBase`/`openGraph`/`twitter` metadata (so round 7's "Copy link" pasted as bare text), no `robots.ts`, no `sitemap.ts` and no `@media print` block. It also closes **all six** of the behaviours round 7 recorded and consciously did not fix.

**Behaviour changes, each with its task and commit**
*   **A shared link unfurls as a card (Task 2, `af54ed5`)**: `metadataBase` plus site-level `openGraph` and `twitter` objects in the root layout, one `SITE_DESCRIPTION` constant feeding `<meta name="description">` and both cards. Round 6's `title.template` is preserved untouched. The description changed from "Manage and filter your jokes" to a sentence that reads as a card subtitle; `icons.icon` still points at round 7's 290-byte SVG mark, not the wordmark.
*   **A crawler is told what is worth indexing (Tasks 3 and 4, `fdc940d`, `51fb35e`)**: `/robots.txt` allows `/` and disallows `/api/`, `/add-joke`, `/edit-joke/`, `/manage` and `/auth` — every route that needs a session to mean anything — and names the sitemap. `/sitemap.xml` carries two entries, `/` and `/jokes`, with **no `lastModified`**: stamping it with the build time would report a change on every deploy whether or not anything changed, which is exactly the signal it is supposed to carry. Route count 12 → 14.
*   **A throw in a route no longer strands the user (Task 5, `a88bb45`, extended in `a8a46f9`)**: `src/app/error.tsx` replaces the failed segment while the root layout — navigation, theme, toaster — stays mounted, and offers `reset()`. It deliberately does **not** render `error.message`: six of seven routes fetch from Firestore and a raw exception string can carry a document id or a rule name; `digest` goes to the console and the server logs.
*   **A throw in the root layout has a screen (Task 6, `a0954ea`)**: `global-error.tsx` replaces the whole document, so it renders its own `<html>`/`<body>` and imports `globals.css` itself (the layout that failed took its stylesheet with it). Two consequences accepted rather than worked around: the page is always light, because adding a theme provider means mounting the provider that may be what just crashed; and it renders in the system sans face, because the font variable lived on the layout's body. `Button` is safe there — a plain forwardRef with no hooks and no context.
*   **`/jokez` gets a real 404 (Task 7, `7a78d24`)**: an `EmptyState` with a compass, "That page doesn't exist." and a "Browse jokes" button, inside the root layout so the navigation bar is present. A **server** component — no state, no handler, so keeping it one costs the visitor nothing. Round 7 made joke URLs shareable, which is what turned a mistyped or stale share link into a real path here.
*   **The orphaned tooltip primitive and ten unused packages are gone (Task 8, `227ca93`)**: `src/components/ui/tooltip.tsx` — orphaned since round 7's Task 4 removed its only consumer and recorded as "no action" — plus `@radix-ui/react-{tooltip,accordion,avatar,checkbox,menubar,progress,scroll-area,tabs}`, `react-day-picker` and `patch-package` (which had no `postinstall` and no `patches/`). `date-fns` stays: three importers, even though `react-day-picker` peer-depended on it. `next.config.ts` is now an empty config object — the `picsum.photos` remote image pattern predated the placeholder jokes round 3 deleted.
*   **The feed announces the empty-state hint, not just its headline (Task 9, `c907a50`)**: round 7 recorded that `/jokes`'s status region spoke `title` only, so a search matching nothing said *"No jokes matched “aardvark”."* and never the sentence explaining that search matches whole keywords of three or more letters. `emptyFeedAnnouncement` joins the two in the module that writes them, and a test pins the region to `${title} ${hint}` **for every branch**, so the region cannot contradict the screen.
*   **The joke page announces a joke it could not load (Task 10, `bf92344`)**: round 7's third silent site. Both the error branch and the not-found branch now carry a `role="status"` region that **mounts empty and is filled from an effect** once the load settles — round 6's rule, that a live region mounted with its message never announces. A failure outranks a missing joke, because "we could not reach the server" is actionable and "there is no such joke" is not. The visible strings did not change: they moved into `jokeStatus.ts` so the region and the screen have one source.
*   **The copied label resets from the last press (Task 11, `b2f2fa4`)**: `CopyLinkButton`'s two-second timer is held in a ref, cleared on unmount and cleared before each new press. Press twice inside the window and the label used to inherit the first press's deadline, reverting 100ms after a copy the toast said had just happened; a copy that *failed* after one that worked left the label reading "Copied" under a toast saying otherwise. Both gone. No `document.execCommand` fallback, on purpose (see the deferrals).
*   **The share control carries Back's weight (Task 12, `23eb12c`)**: `ghost` → `outline`, matching `BackToFeedButton` in the same row. Ghost has no border and no background until hover, and there is no hover on a phone, so the page's own sharing control read as plain text next to a real button. **Rejected alternative, recorded:** demoting Back to `ghost` would put sharing above returning in the hierarchy and undo round 5's Task 14. Equal weight asserts nothing untrue.
*   **No trailing gap after the account icon (Task 13, `98fb2fe`)**: `sm:mr-2` → `lg:mr-2` on the account trigger's `UserCircle`. Round 7's Task 8 moved that label to the large breakpoint and left the margin on the small one, so between 640px and 1023px the icon carried 8px of trailing space with a 1px `sr-only` box after it. The three nav-item icons and Login keep the small-breakpoint margin: their labels appear there.
*   **The dialog close button stays on screen (Task 14, `690e16d`)**: round 7's Task 10 made `DialogContent` scroll, which left the `absolute right-4 top-4` close button scrolling away with the content. **`sticky` is not the fix** — the panel was a grid and an auto-placed grid item sticks inside its own grid area, which for the last child is the bottom of the scrolled content. The scroll moved one level in: the panel is a fixed-height flex column, an inner wrapper carries the header/body/footer grid, the `gap-4` and the overflow (with `min-h-0`, without which a flex item will not shrink below its content height). Nothing about the panel's size, position or animation changed.
*   **Focus rings a mouse click does not leave behind (Task 15, `f8a84db`)**: `SelectTrigger` (the "Own rating" and "Usage Status" controls) and `ToastAction`/`ToastClose` converted from `focus:` to `focus-visible:`, the rule round 4's Task 6 established on the joke cards. Clicking a select and picking a value used to leave a 2px purple ring beside a second trigger that had none, which reads as an error state. `focus:opacity-100` on the toast close button **stays on plain `focus:`** — round 5 made it hover-revealed above `sm`, and a keyboard user arriving via click-then-tab must not find an invisible control. Only the ring is conditioned.
*   **A joke you can print (Task 16, `3e0fbec`, corrected in `9f8c6d5` and `e9fc662`)**: an unlayered `@media print` block at the end of `globals.css` plus `print:hidden` on `ToastViewport` (a portal at the end of `<body>`, outside every landmark, so no selector in the block reaches it without naming a Radix internal). The sheet loses the sticky nav, every button, link colour and underline, and all box shadows; joke cards get `break-inside: avoid` so a page break cannot fall between the setup and the punchline; and the feed card's six-line clamp is unset, because on paper there is no grid row to keep even and the clamp ended a long joke mid-sentence.

**The measured figures — computed, not estimated**
*   **`/logo.png` is 1449×324, not 1395×312** — read from the PNG header (round 7's entry recorded the smaller pair; corrected here). At **4.47:1** it is a banner, and X crops a `summary_large_image` to roughly 1.91:1, which would slice the top and bottom off the only thing in the image. The card is `summary`, which letterboxes the whole wordmark next to the title. The file is 209,273 bytes and served raw from `public/`, so it costs a page load nothing — only an unfurler fetches it.
*   **Print colour needs two moves, and the second is the one that matters.** Browsers drop background colours when printing but keep foreground ones, and the dark-mode class stays on `<html>`. Dark `--foreground` (`0 0% 98%`) prints at **≈1.04:1** on paper — invisible. Pinning `html`/`body` is not enough, because card and joke text colour themselves through token-backed utilities rather than inheriting, so the block redeclares the tokens for `:root` **and** `.dark` at once: `--foreground`/`--card-foreground`/`--popover-foreground` to black, `--muted-foreground` to `0 0% 30%` (**≈8.5:1** on paper, against **≈2.5:1** for the dark value it replaces), and `--primary`/`--accent-foreground` to black — dark-mode purple `270 60% 72%` is **≈2.65:1** on white paper, and it is what stars, metadata icons and the category badge read (`e9fc662`).
*   **What makes the print block win is `!important`, not its position.** It is last in `globals.css` but not in the compiled sheet — Tailwind emits the responsive variant groups and the `print:hidden` utility after it. The two page-break rules are the deliberate exception and carry no `!important`: nothing in the app sets `break-inside` through a utility, so there is no competing declaration to lose to.
*   **The dependency pass removed 32 entries from `package-lock.json`** — ten direct dependencies and twenty-two transitive ones.
*   **Test accounting, 282 → 305 in 16 → 18 files**: `siteUrl` 12 (8 planned + 4 from the batch-1 errata), `feedEmptyState` +5, `jokeStatus` 6. Tasks 2–8 and 11–16 add no tests and changed none of these numbers.

**New modules and their suites** (still no React testing library in this repo, and this round did not add one — logic goes into pure `src/lib/` modules and is tested there, the way rounds 3–7 met the same obligation; `src/lib/` is outside Tailwind's content globs, so nothing there returns a class name)
*   `src/lib/siteUrl.ts` + suite (Task 1) — `resolveSiteUrl`, so tasks 2, 3 and 4 do not each invent their own environment-variable precedence: `NEXT_PUBLIC_SITE_URL`, then `VERCEL_PROJECT_PRODUCTION_URL`, then `VERCEL_URL`, then the dev origin. Takes its environment as an argument, so the precedence is testable without touching `process.env`; a bare hostname gains an `https://` scheme, an explicit `http` origin is never rewritten (a tunnel or a LAN address), trailing slashes go, and a variable that was declared and left blank is treated as unset. `SITE_URL` reads named `process.env` members rather than the object, because Next inlines only static member access.
*   `src/lib/jokeStatus.ts` + suite (Task 10) — `describeJokeLoadResult` plus the two not-found strings as exported constants, since the branch renders them visibly as well. `''` for a loaded joke: the page then has an `h1` with the joke in it and the route announcer has already spoken the title, so "joke loaded" on top of that is noise. Any terminal punctuation is replaced with a single period, not just a period — the rule `feedEmptyState` learned when a period-only version turned "Failed!" into "Failed!.".
*   `emptyFeedAnnouncement` in `src/lib/feedEmptyState.ts` + cases in the existing suite (Task 9) — the headline and the hint as one utterance, because a live region has no layout: the two lines are one announcement or the second reaches nobody.
*   **Four App Router convention files**, none of them testable here: `error.tsx`, `global-error.tsx`, `not-found.tsx` (Tasks 5–7) and the two metadata routes `robots.ts`/`sitemap.ts` (Tasks 3–4). A React error boundary cannot be exercised without a renderer and the metadata routes are object literals whose only computed value is Task 1's tested function.

**Audited and found correct — so the next round does not re-audit it**
*   **`loadedFilters` untouched (Task 9)**: `git diff 767163e..HEAD -- src/app/jokes/page.tsx` contains no changed line mentioning it. `src/contexts/`, `firestore.rules` and the four API routes were **not opened at all** this round — the range diff over those paths is empty.
*   **`focus:ring-2` survives in exactly two files, both deliberate**: the skip link in `src/app/layout.tsx` (never clicked, documented in the file since round 4) and `src/components/ui/badge.tsx` (inert — no badge in this app is focusable, and the removable filter chip's × is its own button that already uses `focus-visible:`). Do not "fix" them.
*   **`date-fns` has three importers** and stays; `command.tsx` was kept on the strength of its export alone (see the deferrals for what that costs).
*   **`SITE_URL` resolves to the dev origin under vitest**, and a test pins it: none of the three variables is set in the test runner, because `.env.production.local` is Next's to load. A refactor that reads the environment some other way, or resolves lazily, now breaks in the suite rather than in production metadata.

**Errata and judgment calls inside the round**
*   **`a8a46f9` (batch-1 review)** — three findings. (1) **A canonical origin was missing**: with only the Vercel variables set, `metadataBase`, the sitemap and robots.txt's `Sitemap:` line would all advertise a `*.vercel.app` host rather than the one users see, so `NEXT_PUBLIC_SITE_URL=https://jokehub.org` is now stated in `.env.production.local` with the reasoning beside it. (2) **A malformed variable would have thrown inside the layout**: the resolved string goes straight to `new URL`, so a bare scheme or a host with a space in it produced a TypeError during rendering with a stack naming the layout and never the variable. `isUsableOrigin` turns that into a fall-through to the next candidate, and a malformed value is now treated exactly like an unset one (four new tests). (3) **The route boundary announced nothing**: it swaps a segment's content without moving focus and without changing the document title, so a screen reader user got silence where the page used to be — it now has a polite `sr-only` region mounted empty and filled from the effect, with the headline shared as one constant. Also in the commit: the OG image dimensions corrected to the file's real ones, the Twitter card downgraded to `summary` (figures above), and a note on why `global-error.tsx` does not reuse `EmptyState` — it is a full-viewport treatment because the whole app is gone, where `EmptyState` is an inline block sized to sit inside a page that still exists.
*   **`f888d48` (batch-2 review)** — three documentation nits, no behaviour: `describeFeedStatus`'s third parameter renamed `emptyTitle` → `emptyAnnouncement` (it is the whole empty state now, not half of it), `EmptyState`'s comment updated to say the page's region speaks the headline *and* its hint, and a test title that claimed the function "never returns an empty string" while asserting that it does — it now says what it checks: nothing is invented when both strings are blank.
*   **`9f8c6d5` (batch-3 review)** — three findings, all in Task 14 and Task 16 territory. **Dark mode printed a near-white page** (the token remap above; pinning `html`/`body` alone was not sufficient). **A clamped joke printed truncated** — the six-line clamp plus the card's hidden overflow ended a long joke mid-sentence with no way to read the rest, so all three parts of the clamp are unset in print. And **the close button's glyph opacity was on the wrong element**: `opacity-70` on the button composited its own opaque background along with the icon, so joke text slid visibly through the fill it was there to provide. The opacity rides the `svg` child instead. Variant order is load-bearing on the hover rule and reads backwards — the child selector first and `hover` second is what lands the hover on the button and the opacity on the glyph; the other way round fires only over the sixteen pixels of the icon.
*   **`e9fc662`** — the print token map extended to `--primary` and `--accent-foreground` after the first pass covered text and surfaces but not the star glyphs, the metadata icons or the category badge text, all of which read those two.
*   **`4e8ce03`** — the plan's own test-count table corrected. The batch-1 errata added four tests and landed *before* Task 7, so every task from there on ran against **305**, not the planned 301; Tasks 14, 15, 16 and Final verification were updated and the per-task increments inside Tasks 9–13 deliberately left as written, because they are what the plan said before the errata existed.

**Known behaviour recorded, not fixed**
*   **Three of round 7's recorded live-region cases are still open**, and none is a regression: the first "Loading jokes…" on `/jokes` is silent (while `authLoading` is true the page early-returns `PageLoading`, so the region mounts already carrying its text); a "Load More" that appends zero jokes announces nothing, because the count does not change; and a failed initial fetch produces two announcements, an assertive toast and a polite region, both true and deliberately different in sensitivity.
*   **`not-found.tsx` exports no `metadata`**, so a 404 keeps the root title "Joke Hub". `not-found.tsx` is not a route segment and Next does not read a metadata export from it. Recorded rather than attempted.

**Accepted deferrals (round 8)**
*   *Per-joke Open Graph cards and a per-joke `<title>` on `/joke/[jokeId]`* — **fourth round deferred** (rounds 6 and 7 both named it). Task 2 gives every route the *site* card, which is the difference between a bare URL and a recognisable link; a card carrying the joke's own text needs `generateMetadata` reading Firestore through the Admin SDK on a route that is client-fetched only. That is a data-layer change — a server read, a caching decision, and a product call about what the card should say, given that the setup is the interesting half and the punchline is the half you must not spoil. It deserves its own round.
*   *A purpose-built 1200×630 Open Graph image* — Task 2 points at the wordmark, which a card slot letterboxes. A 1200×630 card is a design asset, and inventing one in a code round produces exactly the stretched-logo card this task exists to improve on.
*   *Per-joke entries in the sitemap* — same blocker as the per-joke title, plus a build-time list that goes stale the moment somebody adds a joke. `/jokes` links to every joke, so a crawler reaches them all one hop from an entry that is already there.
*   *`loading.tsx` per route* — every page here is a client component that fetches in an effect and already renders `PageLoading` while it does. A route-level `loading.tsx` would paint a *second* spinner for the segment's payload and then swap it for the page's: a flash, not feedback. Worth revisiting only if any of these routes ever fetch on the server.
*   *`.rounded-lg` as the print page-break hook* — it works today and it is the wrong kind of selector: presentational, so it catches anything else rounded the same way (the dialog panel, a rounded image) and silently stops protecting a card the day somebody changes that card's corner radius. The honest fix is a hook the card owns — a data attribute on `JokeListItem`, or a print class at the card's call sites — which means touching every surface that renders a joke card, with its own print check.
*   *`CommandDialog`'s panel classes no longer reach the scrolling element* — Task 14 moved the scroll onto an inner wrapper, and `command.tsx` passes a hidden-overflow and zero-padding pair into `DialogContent` expecting to land on the element that scrolls. Nothing is broken, because `CommandDialog` is exported and instantiated nowhere. Two ways out, neither a review erratum: delete it as dead code, or give `DialogContent` a second class slot for the wrapper. Whoever builds a command palette hits this first, which is the right moment to decide.
*   *The dialog's scrollbar sits a padding's width inside the panel edge* — the same Task 14 move: the panel carries the padding, the wrapper scrolls. Cosmetic, and only visible while there is something to scroll. Fixing it redistributes the padding Task 14 was careful not to disturb, so the panel's border-to-content spacing would depend on three call sites agreeing instead of one.
*   *Content Security Policy and other security headers* — real and absent, but a CSP for an app loading Firebase Auth, Firestore's streaming transport and Google Fonts is a round of its own with a real chance of breaking auth in production, and it cannot be validated from this environment. Task 8 leaves `next.config.ts` as a clean empty object, which is a good place to start that round.
*   *A `document.execCommand` clipboard fallback for insecure origins* — round 7 recommended against it and Task 11 keeps that: the destructive toast points at the address bar, which is always true, and the API it would call is deprecated.
*   *Repositioning the dialog for the on-screen keyboard* — round 7's reason holds. Following the *visual* viewport as a keyboard opens needs a `window.visualViewport` listener and a positioning override on a Radix-owned element; Task 14 fixes the short *layout* viewport, which is the case that produced an unreachable button.
*   *Live regions for the home teaser and the community-ratings list* — both resolve once per page load, where Next's route-title announcer already reports the navigation. `/joke/{id}`'s not-found branch was different because it is the destination of a shared link to a joke that no longer exists, which is why Task 10 covered it.
*   *`src/app/favicon.ico`* — round 7 reviewed it and decided to keep it: Next serves it at `/favicon.ico` for browsers that do not take an SVG icon. Unchanged, and re-recorded so round 9 does not re-audit it.
*   *The dangling `aria-describedby` in `src/components/ui/form.tsx`* — round 6's deferral stands verbatim: an unresolvable idref is ignored per accname, and fixing it properly needs a two-pass registration in `FormItem` for zero user-facing gain.
*   *Renaming `/manage` to `/import`* — **fourth round**. The nav label, page title and filler card were all fixed in round 5; the rename is a redirect plus every inbound link for no additional user-visible gain.
*   *Removing `framer-motion` from `src/app/add-joke/page.tsx`* — **sixth round running**, and this round adds no new argument. Round 7 tried to measure the bundle cost and could not: Next 16 with Turbopack prints no per-route size table.
*   *Optimistic updates, and an unsaved-changes guard on the edit form* — rounds 5, 6 and 7 all deferred both; nothing in this round's audit changes the reasoning.

## NEXT
*   **Deploy Firestore rules/indexes** — `npm run firestore:deploy`. Still not done: the firebase CLI in this environment is unauthenticated, and rules cannot be compiled locally either (no `firebase login` credentials, and the Firestore emulator needs a Java runtime that is not installed). Needs Marco to run `firebase login --no-localhost` or supply a service-account JSON. The round-1/3 rule and index changes are live in git only.
*   Set `JOKEHUB_JARVIS_USER_ID` in Vercel env.
*   **Set `NEXT_PUBLIC_SITE_URL` in Vercel env** (round 8, `a8a46f9`) — `https://jokehub.org`, matching `.env.production.local`. Without it `src/lib/siteUrl.ts` falls back to `VERCEL_PROJECT_PRODUCTION_URL`/`VERCEL_URL`, and `metadataBase`, every sitemap entry and robots.txt's `Sitemap:` line would advertise a `*.vercel.app` host instead of the domain users see. Nothing breaks, but every absolute URL the site emits names the wrong origin.
*   **A data-layer round for per-joke metadata** (round 8 deferral, deferred since round 6) — `generateMetadata` on `/joke/[jokeId]` reading Firestore through the Admin SDK, which unlocks the per-joke `<title>`, the per-joke Open Graph card and per-joke sitemap entries in one go, along with the product call about how much of a joke a card may spoil. The largest single item the last three rounds have handed forward.
*   **A security-headers round** (round 8 deferral) — CSP and friends in `next.config.ts`, which Task 8 left as a clean empty object. Cannot be validated from this environment: Firebase Auth, Firestore's streaming transport and Google Fonts all need to be allowed, and getting it wrong breaks auth in production.
*   **Accepted deferrals from round 3** (reviewed and consciously not fixed):
    *   *§1.6 residual rating-aggregate forgery risk* — the rule bounds each aggregate delta but does not cross-check it against the `jokeRatings` collection, so a caller can still nudge a joke's aggregates one plausible rating at a time. Closing it requires moving `submitUserRating` behind an authenticated API route on `adminDb.runTransaction` and restricting joke updates to the owner branch; documented as future work in `firestore.rules:76-86`.

## NEXT (older)
*   (To be defined by the user for Joke Hub application features).
